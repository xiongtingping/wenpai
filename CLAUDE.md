# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 开发环境与构建

### 常用命令
```bash
# 开发环境
npm run dev                 # 启动开发服务器 (localhost:5173)
npm run build              # 构建生产版本
npm run build:check       # 构建前检查 (包含类型检查和系统检查)
npm run lint              # ESLint检查
npm run lint:fix         # 自动修复ESLint问题  
npm run type-check       # TypeScript类型检查
npm test                 # 运行Jest测试

# CSS治理系统
npm run css:governance:check           # CSS规范检查
npm run css:governance:fix            # 自动修复CSS违规
npm run css:governance:smart-check    # 智能CSS检查
npm run style:check                   # 样式系统检查
npm run tokens:build                  # 构建设计令牌

# 部署相关  
npm run deploy:netlify    # 部署到Netlify
npm run predeploy        # 部署前检查
```

### 技术栈
- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite 7
- **样式系统**: Tailwind CSS + CSS Layers + 设计令牌
- **UI组件**: Radix UI + shadcn/ui
- **状态管理**: Zustand + React Context
- **国际化**: i18next + react-i18next
- **认证**: Authing Guard + Supabase Auth
- **数据库**: Supabase PostgreSQL
- **部署**: Netlify (带Netlify Functions)

## 核心架构

### 项目结构
```
src/
├── components/          # UI组件 (分功能模块组织)
│   ├── ui/             # 基础UI组件库 (shadcn/ui)
│   ├── auth/           # 认证相关组件
│   ├── creative/       # 创作工具组件
│   ├── landing/        # 落地页组件
├── pages/              # 页面组件
├── features/           # 功能模块 (独立的业务逻辑)
│   ├── content-adapter/  # 内容适配器
│   ├── titleGeneration/  # 标题生成
├── services/           # 服务层 (API调用、业务逻辑)
├── hooks/              # React自定义Hooks
├── utils/              # 工具函数
├── stores/             # 状态管理
├── api/                # API封装和请求管理
├── config/             # 配置文件
├── types/              # TypeScript类型定义
└── styles/             # CSS样式文件
```

### 认证架构
- **多重认证系统**: Authing + Supabase双重认证
- **权限控制**: 基于角色的权限系统 (guest/trial/pro/premium)
- **路由守卫**: `AuthGuard`, `ProGuard`, `PremiumGuard`
- **权限组件**: `PermissionGuard`, `UnifiedPermissionWrapper`

### CSS架构治理
- **层级系统**: 使用`@layer`确保样式优先级可控
- **设计令牌**: 所有颜色、间距、字体通过CSS变量统一管理
- **禁止硬编码**: 严格禁止内联样式和硬编码样式值
- **Tailwind + CSS Variables**: 结合Tailwind和自定义CSS变量

### API架构
- **统一请求**: 所有API请求通过`src/api/request.ts`处理
- **环境变量**: API密钥和配置通过环境变量管理
- **错误处理**: 统一错误处理和重试机制
- **代理转发**: Netlify Functions实现API代理和CORS处理

### 数据管理
- **持久化**: 所有数据保存到Supabase
- **用户隔离**: 严格的用户数据隔离机制
- **状态管理**: Zustand统一状态管理（unified-state-store）
- **数据同步**: 统一的数据同步和冲突解决

### 存储架构（2024-01重构）
- **单一数据源（SSOT）**: `src/stores/unified-state-store.ts`
- **兼容层**: `src/stores/compatibility-layer.ts` 保持向后兼容
- **自动迁移**: 应用启动时自动从旧版auth-store迁移数据
- **废弃文件**: `src/stores/auth-store.ts` (已标记@deprecated)
- **迁移指南**: `docs/STORAGE_MIGRATION_GUIDE.md`
- **测试指南**: `docs/STORAGE_MIGRATION_TESTING.md`
- **localStorage Keys**:
  - `wenpai-unified-store` - 主要存储（当前使用）
  - `wenpai-auth-store-v2` - 旧版存储（已废弃）
  - `wenpai-auth-store-v2-backup-*` - 自动备份

### 国际化系统
- **i18next**: 支持中文(zh-CN)和英文(en-US)
- **全局实例**: 在`main.tsx`设置全局i18n实例避免TDZ错误
- **翻译文件**: `src/i18n/locales/`目录下的JSON文件

### 关键约定
- **模块导入**: 使用`@/`别名引用src目录
- **组件命名**: React组件使用PascalCase
- **文件命名**: 普通文件使用kebab-case，组件文件使用PascalCase
- **CSS命名**: BEM方法论 + 设计令牌
- **环境变量**: 敏感配置必须通过环境变量注入

---

# 项目治理规则

## 1. 核心原则

### 1.1 禁止技术债务
严禁patch式修复，必须根因修复。所有修改必须考虑长期可维护性，避免积累技术债务。

### 1.2 禁止硬编码和模拟数据
- 敏感信息必须通过环境变量配置
- 禁止本地模拟数据和降级方案
- 必须使用真实可用的在线API

### 1.3 大规模修复授权
允许大规模代码修复但必须：禁止删除功能、修复导向、向后兼容、逐步改进、全面测试。

## 2. 设计与架构一致性

### 2.1 设计令牌系统
所有UI组件必须使用设计令牌，包括颜色、字体、间距、样式等。确保UI一致性，禁止硬编码样式。

### 2.2 上下文隔离
系统不同功能模块必须进行有效的上下文隔离，确保模块间状态不会交叉污染。

## 3. 问题修复与系统性排查

### 3.1 5W1H根因分析方法论
修复问题前必须使用结构化分析框架：
- **What**：详细描述问题现象和影响范围
- **When**：确定问题发生的时机和触发条件
- **Where**：定位问题发生的具体位置（文件、组件、功能）
- **Who**：识别受影响的用户群体和系统组件
- **Why**：深入分析根本原因，建立因果关系链
- **How**：设计系统性解决方案，评估实施风险

### 3.2 严禁patch式修复
不允许在未梳理全部可能原因的情况下直接修复。必须深入分析并修复根本原因。

### 3.3 换角度分析强制方法论
**强制触发条件**：同一个问题尝试修复3次仍未成功时，必须立即执行换角度分析。

**强制分析维度**（按优先级顺序）：
1. **构建工具层面**：检查Vite/Webpack配置、依赖管理、模块解析
2. **依赖关系层面**：分析循环依赖、导入顺序、版本冲突
3. **运行时环境层面**：检查环境变量、全局对象、浏览器兼容性
4. **模块加载层面**：分析ES模块、CommonJS混用、动态导入
5. **架构设计层面**：重新审视组件结构、服务层次、数据流向

**禁止重复**：
- 禁止在同一维度反复尝试相似解决方案
- 禁止陷入"应该能工作"的思维陷阱
- 禁止忽视构建产物分析和错误日志

**成功指标**：
- 问题得到根本解决，而非临时消除症状
- 修复方案具有可复制性和系统性
- 建立问题模式库，防止类似问题再次发生

### 3.4 多环境验证
修复完成后必须在开发和生产环境下进行自动化测试，确保修复效果有效。

### 3.5 举一反三排查
修复一个问题时，必须系统性排查代码库中是否存在类似问题模式，建立问题模式库。

### 3.6 任务完成验证规范
**每次任务完成后必须执行以下强制性验证**：

1. **修改说明强制要求**：详细解释改了什么，以及为什么这样改
2. **风险评估强制要求**：检查修复工作，列出可能出问题的地方
3. **生产环境循环测试强制要求**：
   - 测试必须在正式的生产环境下进行，禁止开发环境测试
   - 自动执行git push并部署到生产环境
   - 自动读取生产环境控制台日志和错误信息
   - 发现问题时自动进行根因修复
   - 不能只停留在代码层修复成功或构建成功
   - "应该能跑" = 不能跑，必须实际在生产环境验证
   - 发现问题立即进入下一轮修复循环直到完全可用

## 4. 功能模块与系统操作

### 4.1 禁止裁剪功能
禁止未经批准删除、裁剪或减少现有功能模块。

### 4.2 部署前检查
部署前必须进行构建检查，确保一次性成功部署。

## 5. 自动化与规则应用

### 5.1 自动加载规则
执行任务前必须自动加载CLAUDE.md规则，如果加载失败则中止任务。

### 5.2 修复方案验证
输出修复方案前必须验证可行性，避免"盲修"行为。

## 6. 任务跟踪与进度管理

### 6.1 TodoWrite工具强制使用标准
**必须使用TodoWrite的场景**：
- 复杂修复任务（3个以上步骤）
- 涉及多个文件或组件的修改
- 需要分阶段验证的系统性问题
- 用户提供的多项任务清单

### 6.2 任务状态管理规范
**状态转换标准**：
- `pending` → `in_progress`：开始执行前必须更新
- `in_progress` → `completed`：完全完成后立即更新
- **同时只能有一个任务为in_progress状态**

### 6.3 任务粒度控制
**任务分解原则**：
- 单一职责：每个任务只解决一个具体问题
- 可验证完成：任务结果必须可测试和验证
- 时间限制：单个任务不超过30分钟完成时间

## 7. CSS架构治理体系

### 7.1 治理原则
1. 单一真相源(SSOT)：所有样式来源于统一设计令牌
2. 严格层级管理：使用@layer确保优先级可控
3. 零容忍硬编码：禁止任何硬编码样式值
4. 自动化优先：通过工具强制执行规则
5. 影响评估强制：任何修改必须评估全局影响

### 7.2 CSS层级管理
**强制性层级顺序**：`@layer reset, tokens, base, components, utilities, overrides, emergency;`

### 7.3 CSS修复决策框架
**Emergency层级使用条件**：
- 仅在关键功能完全失效时使用
- 必须有明确的临时修复计划
- 需要在24小时内提供正式解决方案

**CSS冲突解决优先级**：
1. 检查@layer层级是否正确
2. 验证CSS导入顺序
3. 确认选择器特异性
4. 最后考虑!important（仅Emergency层）

### 7.4 严格禁止清单
**CRITICAL级别**：
- 全局Transform属性
- 内联样式

**ERROR级别**：
- 硬编码颜色值和尺寸值
- !important滥用（除emergency层外）
- 跨层级样式定义

### 7.5 设计令牌迁移和验证标准

**硬编码检测清单**：
- [ ] 颜色值：检查所有#hex、rgb()、hsl()直接使用
- [ ] 尺寸值：检查px、rem、em等硬编码数值
- [ ] 间距值：检查margin、padding的固定值
- [ ] 字体值：检查font-family、font-size硬编码

**令牌命名约定**：
- 格式：`--{category}-{property}-{variant}`
- 示例：`--color-primary-500`、`--spacing-md`、`--header-height`
- 禁止：简写命名、数字开头、特殊字符

**验证测试方法**：
- 构建时扫描：自动检测硬编码值
- 视觉回归测试：确保令牌替换不影响UI
- 跨浏览器兼容性测试

### 7.6 命名规范体系
**CSS文件**：kebab-case命名法
**BEM结构**：`块__元素--修饰符`
**设计令牌**：`--{category}-{property}-{variant}`
**React组件**：PascalCase命名法
**Hook**：use前缀 + 功能描述

## 8. 数据管理与测试规范

### 8.1 数据持久化
所有数据保存到Supabase，包括：数据持久化、预加载、用户隔离、数据安全性。

### 8.2 调试代码管理标准

**保留标准**：
- 在路由中注册的调试页面
- 被其他组件引用的调试工具
- 系统监控和诊断脚本
- 配置验证文件

**删除标准**：
- 临时测试HTML文件
- 一次性验证脚本
- 未被引用的调试组件
- 过期的性能测试工具

**清理流程**：
1. 使用Grep工具检查文件引用关系
2. 确认文件在git中的提交状态
3. 备份重要调试工具到专门目录
4. 批量删除确认无用的文件

## 9. 技术术语与最佳实践

- **上下文隔离**：严格隔离各模块执行上下文
- **异常处理**：健壮的异常处理机制
- **数据一致性**：确保ACID属性
- **依赖注入**：降低模块耦合度
- **数据泄露防护**：敏感数据加密存储传输
- **负载均衡**：支持高可用性和扩展性
- **服务解耦**：避免直接依赖和紧密耦合
- **事件驱动**：基于事件触发和响应
- **CI/CD**：自动化测试、构建、部署
- **监控与日志**：实时捕获记录系统状态

## 10. 核心执行准则

### 10.1 系统化排查
通过自上而下方式排查与修复，确保根本原因被解决。

### 10.2 综合性修复
修复方案应覆盖所有可能场景，从多角度解决问题。

## 11. 强制执行要求

### 11.1 任务前强制读取
- 每次任务前必须读取CLAUDE.md文件
- 完整理解应用所有规则条目
- 读取失败则立即中止任务

### 11.2 规则执行验证
每次执行时验证核心原则：
✅ 避免技术债务
✅ 避免硬编码和模拟数据
✅ 使用真实可行API
✅ 系统性问题排查
✅ 避免patch式修复
✅ 使用TodoWrite跟踪复杂任务
✅ 遵循CSS架构治理

### 11.3 违规处理机制
发现违规立即停止操作，回滚修改，重新按规则执行。

## 12. 代码库索引文件使用规范

### 12.1 索引文件说明
- `general_index.md`：所有文件列表及功能描述
- `detailed_index.md`：函数、类、组件名称及文档

### 12.2 索引准确性
索引文件可能不是最新的，仅作辅助信息，关键信息必须验证实际代码。

### 12.3 使用原则
- 快速定位问题区域
- 减少重复扫描
- 辅助理解代码库结构
- 双重验证：索引定位 + 实际验证

## 14. TDZ错误修复指南

### 14.1 TDZ错误根因类型
**已验证的TDZ错误根因及解决方案**：

1. **循环依赖型TDZ**：
   - 根因：Vite别名覆盖导致模块间循环依赖
   - 解决：移除problematic别名，使用原生包
   - 实例：@radix-ui/react-slot别名 → House组件TDZ

2. **模块级调用型TDZ**：
   - 根因：模块顶层调用未初始化的变量
   - 解决：延迟加载+安全访问+fallback
   - 实例：contentForms.ts中i18n.t()调用

3. **运行时访问型TDZ**：
   - 根因：访问undefined对象的属性
   - 解决：提供默认值 || {}
   - 实例：globalEnv[key]访问失败

4. **全局实例型TDZ**：
   - 根因：系统性缺少全局变量设置
   - 解决：在main.tsx最早阶段设置全局实例
   - 实例：100+文件使用i18n.t()

### 14.2 TDZ修复原则
- **换角度分析**：从构建工具、模块加载、运行时等不同层面分析
- **避免patch式修复**：找到根本原因而非症状
- **系统性解决**：对于系统性问题采用全局解决方案

## 15. 文档维护规范

### 15.1 CLAUDE.md精简要求
**强制性规则**：CLAUDE.md文档必须保持精简，不得超过500行。

### 15.2 精简原则
- 删除冗余内容和重复说明
- 案例研究只保留核心要点
- 移除详细代码示例
- 压缩多余的子章节
- 保持核心规则的完整性和准确性

### 15.3 定期维护
- 每次修改后检查行数限制
- 定期审查并精简冗余内容
- 优先保留执行性规则，简化说明性内容

---

**执行模式：强制读取 → 严格遵循 → 持续验证 → 违规纠正**
**目标：保持系统稳定、避免破坏、提升可维护性、确保任务可追溯**