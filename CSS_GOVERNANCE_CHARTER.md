# 🏛️ CSS架构治理宪章

## 📜 **治理原则**

### **核心原则**
1. **单一真相源（SSOT）**：所有样式必须来源于统一的设计令牌系统
2. **严格层级管理**：使用@layer确保样式优先级可控
3. **零容忍硬编码**：禁止任何形式的硬编码样式值
4. **自动化优先**：所有治理规则必须通过自动化工具强制执行
5. **影响评估强制**：任何样式修改必须评估对全局的影响

### **治理目标**
- ✅ 消除所有样式冲突和意外覆盖
- ✅ 建立可预测的样式行为
- ✅ 确保设计系统的一致性执行
- ✅ 提高开发效率和代码质量
- ✅ 降低维护成本和技术债务

## 🏗️ **CSS层级管理体系**

### **强制性层级顺序**
```css
@layer reset, tokens, base, components, utilities, overrides, emergency;
```

### **层级定义与职责**

#### **1. Reset Layer（重置层）**
- **职责**：浏览器默认样式重置
- **优先级**：最低
- **允许内容**：normalize.css、reset.css
- **禁止内容**：任何业务逻辑样式

#### **2. Tokens Layer（令牌层）**
- **职责**：设计令牌定义
- **优先级**：第二低
- **允许内容**：CSS变量定义、主题切换
- **禁止内容**：具体样式实现

#### **3. Base Layer（基础层）**
- **职责**：全局基础样式
- **优先级**：中低
- **允许内容**：html、body、全局字体
- **禁止内容**：组件特定样式

#### **4. Components Layer（组件层）**
- **职责**：组件样式定义
- **优先级**：中等
- **允许内容**：.button、.card等组件样式
- **禁止内容**：工具类、修复样式

#### **5. Utilities Layer（工具层）**
- **职责**：工具类样式
- **优先级**：中高
- **允许内容**：.u-center、.u-hidden等工具类
- **禁止内容**：组件特定样式

#### **6. Overrides Layer（覆盖层）**
- **职责**：第三方库样式覆盖
- **优先级**：高
- **允许内容**：Radix UI、Tailwind覆盖
- **禁止内容**：业务逻辑样式

#### **7. Emergency Layer（紧急层）**
- **职责**：紧急修复样式
- **优先级**：最高
- **允许内容**：临时修复、hotfix
- **禁止内容**：长期业务样式
- **特殊要求**：必须有移除计划和时间表

## 🚫 **严格禁止清单**

### **绝对禁止的做法**
1. **硬编码样式值**
   ```css
   /* ❌ 绝对禁止 */
   .button { color: #FF0000; width: 200px; }
   
   /* ✅ 正确做法 */
   .button { color: var(--color-primary); width: var(--size-button-width); }
   ```

2. **全局Transform属性**
   ```css
   /* ❌ 绝对禁止 */
   * { transform: translateZ(0); }
   button { transform: translateZ(0); }
   
   /* ✅ 正确做法 */
   .gpu-accelerated { transform: translateZ(0); }
   ```

3. **!important滥用**
   ```css
   /* ❌ 绝对禁止 */
   .my-style { color: red !important; }
   
   /* ✅ 正确做法 - 仅在emergency层使用 */
   @layer emergency {
     .hotfix-style { color: var(--color-error) !important; }
   }
   ```

4. **内联样式**
   ```jsx
   {/* ❌ 绝对禁止 */}
   <div style={{color: 'red', width: '200px'}} />
   
   {/* ✅ 正确做法 */}
   <div className="text-primary w-button" />
   ```

5. **跨层级样式定义**
   ```css
   /* ❌ 绝对禁止 - 在base层定义组件样式 */
   @layer base {
     .button { padding: 1rem; }
   }
   
   /* ✅ 正确做法 */
   @layer components {
     .button { padding: var(--spacing-4); }
   }
   ```

## 🔍 **强制性检查机制**

### **代码提交前检查**
1. **Stylelint规则检查**：自动检测硬编码、层级违规
2. **设计令牌使用检查**：确保所有样式值来自令牌系统
3. **Transform属性检查**：检测意外的transform使用
4. **层级合规性检查**：验证@layer使用正确性

### **CI/CD集成检查**
1. **样式冲突检测**：自动检测样式优先级冲突
2. **性能影响评估**：检测可能影响布局的性能优化
3. **设计系统一致性**：验证组件样式符合设计规范
4. **回归测试**：确保样式修改不影响现有功能

## ⚖️ **违规处理机制**

### **违规等级**
- **Level 1 - 警告**：使用硬编码值、缺少设计令牌
- **Level 2 - 错误**：跨层级定义、!important滥用
- **Level 3 - 阻断**：全局transform、内联样式
- **Level 4 - 紧急**：破坏核心布局、影响用户体验

### **处理流程**
1. **自动检测**：工具自动发现违规
2. **阻断提交**：Level 3+违规阻断代码提交
3. **强制修复**：提供自动修复建议
4. **人工审查**：复杂违规需要人工审查
5. **培训跟进**：违规者必须接受相关培训

## 📊 **治理效果监控**

### **关键指标**
- **样式冲突数量**：目标为0
- **硬编码样式数量**：目标为0
- **设计令牌覆盖率**：目标为100%
- **层级合规率**：目标为100%
- **自动化检查通过率**：目标为100%

### **定期审查**
- **每周**：样式冲突检测报告
- **每月**：设计系统合规性审查
- **每季度**：CSS架构健康度评估
- **每年**：治理体系优化升级

## 🎯 **实施路线图**

### **Phase 1: 基础建设（1-2周）**
- 建立@layer层级管理
- 配置Stylelint规则
- 创建自动化检测工具

### **Phase 2: 存量治理（2-3周）**
- 重构现有CSS文件
- 消除所有硬编码样式
- 修复样式冲突问题

### **Phase 3: 流程集成（1周）**
- CI/CD集成检查
- 代码审查流程优化
- 团队培训实施

### **Phase 4: 持续优化（持续）**
- 监控治理效果
- 优化检测工具
- 更新治理规范

---

**本宪章具有最高优先级，所有CSS相关工作必须严格遵守。违反本宪章的代码将被自动阻断，不得合并到主分支。**
