import React, { ReactElement, CSSProperties, ReactNode, ForwardRefRenderFunction } from 'react';
export interface IRenderer {
    container: Element;
    element: ReactElement;
}
declare function render(renderer: IRenderer): void | Element | React.Component<any, any, any>;
declare function unmount(container: Element): void;
export { React, CSSProperties, ReactNode, render, unmount, ReactElement, ForwardRefRenderFunction };
