/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deactivateLicense } from "../../funcs/deactivateLicense.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeactivateLicenseRequest$inboundSchema,
};

export const tool$deactivateLicense: ToolDefinition<typeof args> = {
  name: "deactivate-license",
  description: `Deactivate a license key instance.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deactivateLicense(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
