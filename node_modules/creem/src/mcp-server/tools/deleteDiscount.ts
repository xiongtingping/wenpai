/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deleteDiscount } from "../../funcs/deleteDiscount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDiscountRequest$inboundSchema,
};

export const tool$deleteDiscount: ToolDefinition<typeof args> = {
  name: "delete-discount",
  description: `Delete a discount.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deleteDiscount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
