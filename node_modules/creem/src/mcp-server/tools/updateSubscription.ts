/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateSubscription } from "../../funcs/updateSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSubscriptionRequest$inboundSchema,
};

export const tool$updateSubscription: ToolDefinition<typeof args> = {
  name: "update-subscription",
  description: `Update a subscription.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await updateSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
