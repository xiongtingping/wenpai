/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { activateLicense } from "../../funcs/activateLicense.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ActivateLicenseRequest$inboundSchema,
};

export const tool$activateLicense: ToolDefinition<typeof args> = {
  name: "activate-license",
  description: `Activates a license key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await activateLicense(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
