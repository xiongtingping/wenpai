/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrieveCheckout } from "../../funcs/retrieveCheckout.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveCheckoutRequest$inboundSchema,
};

export const tool$retrieveCheckout: ToolDefinition<typeof args> = {
  name: "retrieve-checkout",
  description: `Retrieve a new checkout session.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrieveCheckout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
