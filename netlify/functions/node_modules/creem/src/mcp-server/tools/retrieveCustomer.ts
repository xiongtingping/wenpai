/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrieveCustomer } from "../../funcs/retrieveCustomer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveCustomerRequest$inboundSchema,
};

export const tool$retrieveCustomer: ToolDefinition<typeof args> = {
  name: "retrieve-customer",
  description: `Retrieve a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrieveCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
