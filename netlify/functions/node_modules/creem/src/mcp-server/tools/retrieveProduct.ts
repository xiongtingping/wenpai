/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrieveProduct } from "../../funcs/retrieveProduct.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveProductRequest$inboundSchema,
};

export const tool$retrieveProduct: ToolDefinition<typeof args> = {
  name: "retrieve-product",
  description: `Retrieve a product`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrieveProduct(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
