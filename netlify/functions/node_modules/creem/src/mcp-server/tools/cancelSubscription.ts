/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { cancelSubscription } from "../../funcs/cancelSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CancelSubscriptionRequest$inboundSchema,
};

export const tool$cancelSubscription: ToolDefinition<typeof args> = {
  name: "cancel-subscription",
  description: `Cancel a subscription.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await cancelSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
