/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchTransactions } from "../../funcs/searchTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchTransactionsRequest$inboundSchema,
};

export const tool$searchTransactions: ToolDefinition<typeof args> = {
  name: "search-transactions",
  description: `List all transactions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
