# 🏛️ CSS代码审查强制清单

## 📋 **审查前准备**

### **自动化检查确认**
- [ ] ✅ CSS治理强制执行器检查通过
- [ ] ✅ Stylelint治理规则检查通过  
- [ ] ✅ CI/CD管道所有检查通过
- [ ] ✅ 无阻断级别违规（CRITICAL/ERROR）

### **文档确认**
- [ ] 📚 已阅读CSS治理宪章
- [ ] 📚 了解当前修改的业务背景
- [ ] 📚 确认修改范围和影响面

---

## 🚨 **CRITICAL级别检查（必须100%通过）**

### **全局Transform检查**
- [ ] 🚨 **无全局transform属性**
  ```css
  /* ❌ 绝对禁止 */
  * { transform: translateZ(0); }
  button { transform: translateZ(0); }
  
  /* ✅ 正确做法 */
  .gpu-accelerated { transform: translateZ(0); }
  ```

### **内联样式检查**
- [ ] 🚨 **无内联样式使用**
  ```jsx
  {/* ❌ 绝对禁止 */}
  <div style={{color: 'red'}} />
  
  {/* ✅ 正确做法 */}
  <div className="text-error" />
  ```

### **布局破坏性检查**
- [ ] 🚨 **无可能导致按钮居中问题的样式**
- [ ] 🚨 **无可能影响弹窗定位的样式**
- [ ] 🚨 **无破坏响应式布局的样式**

---

## ❌ **ERROR级别检查（必须通过）**

### **设计令牌使用检查**
- [ ] ❌ **无硬编码颜色值**
  ```css
  /* ❌ 错误 */
  color: #FF0000;
  background: rgb(255, 0, 0);
  
  /* ✅ 正确 */
  color: var(--color-error);
  background: var(--color-error-bg);
  ```

- [ ] ❌ **无硬编码尺寸值**
  ```css
  /* ❌ 错误 */
  width: 200px;
  margin: 16px;
  
  /* ✅ 正确 */
  width: var(--size-button-width);
  margin: var(--spacing-4);
  ```

### **CSS层级管理检查**
- [ ] ❌ **所有样式都在正确的@layer中**
  ```css
  /* ✅ 正确的层级使用 */
  @layer components {
    .button { ... }
  }
  
  @layer utilities {
    .u-center { ... }
  }
  ```

- [ ] ❌ **层级顺序正确**
  ```css
  /* ✅ 正确的顺序 */
  @layer reset, tokens, base, components, utilities, overrides, emergency;
  ```

### **!important使用检查**
- [ ] ❌ **!important仅在emergency层使用**
  ```css
  /* ❌ 错误 */
  .my-style { color: red !important; }
  
  /* ✅ 正确 */
  @layer emergency {
    .hotfix { color: var(--color-error) !important; }
  }
  ```

---

## ⚠️ **WARNING级别检查（建议通过）**

### **代码质量检查**
- [ ] ⚠️ **CSS属性顺序符合规范**
- [ ] ⚠️ **选择器复杂度合理（≤4层）**
- [ ] ⚠️ **嵌套深度合理（≤3层）**
- [ ] ⚠️ **类名符合命名规范**

### **性能影响检查**
- [ ] ⚠️ **无低性能动画属性**
  ```css
  /* ❌ 低性能 */
  animation: slide 1s ease-in-out;
  @keyframes slide {
    from { width: 0; }
    to { width: 100px; }
  }
  
  /* ✅ 高性能 */
  animation: slide 1s ease-in-out;
  @keyframes slide {
    from { transform: translateX(-100%); }
    to { transform: translateX(0); }
  }
  ```

- [ ] ⚠️ **GPU加速使用合理**
- [ ] ⚠️ **CSS文件大小合理**

### **可访问性检查**
- [ ] ⚠️ **有适当的焦点样式**
- [ ] ⚠️ **颜色对比度充足**
- [ ] ⚠️ **支持减少动画偏好**

---

## 🎯 **业务逻辑检查**

### **功能完整性**
- [ ] 🎯 **修改解决了原始问题**
- [ ] 🎯 **无引入新的布局问题**
- [ ] 🎯 **在所有支持的设备尺寸下正常工作**
- [ ] 🎯 **在所有支持的浏览器中正常工作**

### **设计一致性**
- [ ] 🎯 **符合设计系统规范**
- [ ] 🎯 **与现有组件风格一致**
- [ ] 🎯 **主题切换正常工作**

### **向后兼容性**
- [ ] 🎯 **不破坏现有功能**
- [ ] 🎯 **不影响其他组件**
- [ ] 🎯 **API接口保持稳定**

---

## 📊 **影响评估检查**

### **范围评估**
- [ ] 📊 **明确修改影响的组件范围**
- [ ] 📊 **评估对全局样式的影响**
- [ ] 📊 **确认无意外的样式继承**

### **风险评估**
- [ ] 📊 **识别潜在的回归风险**
- [ ] 📊 **评估性能影响**
- [ ] 📊 **考虑维护成本**

### **测试覆盖**
- [ ] 📊 **有相应的视觉回归测试**
- [ ] 📊 **有相应的单元测试**
- [ ] 📊 **有相应的集成测试**

---

## 🔧 **修复建议模板**

### **违规修复建议**
```markdown
## 🚨 发现违规：[违规类型]

**问题描述：**
[具体描述违规内容]

**修复建议：**
```css
/* ❌ 当前代码 */
[违规代码]

/* ✅ 建议修复 */
[修复代码]
```

**影响评估：**
- 影响范围：[具体范围]
- 风险级别：[高/中/低]
- 修复优先级：[立即/本周/下周]
```

### **性能优化建议**
```markdown
## 🚀 性能优化建议

**优化点：**
[具体优化建议]

**预期收益：**
- 性能提升：[具体数据]
- 用户体验改善：[具体描述]
```

---

## ✅ **审查通过标准**

### **必须满足的条件**
1. ✅ **所有CRITICAL和ERROR级别检查100%通过**
2. ✅ **自动化检查全部通过**
3. ✅ **业务功能正常工作**
4. ✅ **无明显的性能回归**
5. ✅ **符合设计系统规范**

### **审查签名**
```markdown
## 🏛️ CSS治理审查签名

**审查员：** [姓名]
**审查时间：** [时间]
**审查结果：** ✅ 通过 / ❌ 需要修改

**审查总结：**
[审查总结和建议]

**下次审查要求：**
[如果需要修改，列出具体要求]
```

---

**⚠️ 重要提醒：本清单中的CRITICAL和ERROR级别检查项是强制性的，任何一项不通过都将阻断代码合并。请确保在提交审查前已经通过所有自动化检查。**
