---
type: "always_apply"
---

{
  "name": "自动重启开发服务器并打开验证页面",
  "severity": "warning",
  "description": "每当代码发生变更，尤其是涉及功能逻辑、配置项、界面结构、AI调用等模块，系统必须自动重启开发服务器（如 `npm run dev`），并自动打开默认或指定的页面地址进行验证，确保变更实时生效。"
}