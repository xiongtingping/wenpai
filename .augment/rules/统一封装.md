---
type: "always_apply"
---

{
  "name": "模块接入必须使用统一封装",
  "severity": "error",
  "description": "所有 API 请求必须通过统一封装模块（如 request.ts），认证必须使用 useUnifiedAuth()，权限判断必须使用 usePermission()，行为追踪必须统一封装 track()。禁止使用裸调用或多套系统并存。"
}