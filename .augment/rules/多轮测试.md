---
type: "always_apply"
description: "Example description"
---
{
  "name": "任务执行必须包含自动化、多轮验证流程",
  "severity": "critical",
  "description": "所有涉及修复、重构、配置、集成的任务，必须自动执行构建、运行、或网络请求等完整验证流程，禁止仅返回静态建议或代码片段。系统应自动重启开发服务器（如执行 npm run dev），并打开默认或指定页面进行验证，确保变更已生效。验证范围必须覆盖构建检查、类型检查、E2E 测试、AI功能调用、权限校验、页面访问、控制台日志与用户流程。至少通过 3 轮自动验证方可视为完成，若存在页面报错、加载异常或验证失败，必须立即修复并重新验证。任务未完成验证不得合并或发布。"
}