---
type: "agent_requested"
description: "Example description"
---
{
  "name": "🧪 测试与部署执行全规则",
  "severity": "critical",
  "description": "【测试规范】\n\n✅ 基本原则\n- 所有测试必须基于真实逻辑场景，不得停留在代码表层结构的“假测试”（如仅覆盖接口调用、不验证业务逻辑）；\n- 测试必须验证以下内容之一，才视为有效：\n  - 实际状态变化（如状态管理、后端数据写入）；\n  - 接口调用完整性与响应结构；\n  - 权限判断 / 分支逻辑是否按预期执行；\n  - 渲染输出是否符合业务预期；\n\n❌ 明确禁止\n- 禁止提交无实际断言、无状态验证的“假测试”；\n- 禁止以提高覆盖率为目标编写无效测试逻辑；\n- 禁止保留未启用、未被引用的 test 文件；\n\n🔁 清理机制\n- 测试完成后，必须手动清理：\n  - 所有临时 test 文件；\n  - 无需保留的 mock 文件、断言工具、测试数据等；\n- 若测试模块与原模块合并，请统一收敛至 `/__tests__` 或 `*.spec.tsx`、`*.test.ts` 结构中，避免结构混乱；\n- 请勿将测试逻辑遗留于业务模块内部（如 `src/module/Foo.tsx` 中残留 `test(...)` 函数）；\n\n🔐 冻结与交付要求\n- 对已验证通过的逻辑模块，请添加注释标记：\n```ts\n// ✅ TESTED: 已完成真实业务验证，测试时间 YYYY-MM-DD\n// 🔒 LOCKED: 如需修改请重新创建测试模块，不得修改原 test 框架\n```\n\n【测试页面与文档限制】\n1. 除非任务明确要求创建测试页面，否则禁止在开发或重构过程中额外生成任何测试页面（包括 demo 页面、临时调试页面、mock 页面等）。\n2. 除非任务明确要求输出总结文档，否则禁止生成开发总结、过程说明、额外的 README、回顾文档等。\n3. 在不影响功能完整性的前提下，优先直接交付可运行代码，减少冗余文件和无关说明。\n4. 保证项目目录整洁，禁止保留无引用、无调用的测试代码或调试文件。\n\n【任务执行自动化与多轮验证】\n- 所有涉及修复、重构、配置、集成的任务，必须自动执行构建、运行、或网络请求等完整验证流程，禁止仅返回静态建议或代码片段。\n- 系统应自动重启开发服务器（如执行 npm run dev），并打开默认或指定页面进行验证，确保变更已生效。\n- 验证范围必须覆盖构建检查、类型检查、E2E 测试、AI 功能调用、权限校验、页面访问、控制台日志与用户流程。\n- 至少通过 3 轮自动验证方可视为完成，若存在页面报错、加载异常或验证失败，必须立即修复并重新验证。\n- 任务未完成验证不得合并或发布。"
}

"keywords": ["test", "mock", "demo", "验证", "构建", "E2E", "自动化"]