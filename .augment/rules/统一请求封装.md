---
type: "always_apply"
---

{
  "name": "模块接入必须统一封装，不得私自调用",
  "severity": "error",
  "description": "所有模块接入必须使用系统指定的统一封装。HTTP 请求必须通过 `request.ts` 中封装的 axios 实例发出，禁止使用裸 fetch、axios.create() 或私自封装请求模块。认证必须使用 `useUnifiedAuth()`，权限判断必须使用 `usePermission()`，用户行为追踪必须使用统一的 `track()` 方法。严禁多套系统并存、直接调用底层接口、或绕过统一封装的逻辑路径。"
}